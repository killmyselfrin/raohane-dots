# Raohane fingerprint-only PAM stack.
# RaohaneLockContext runs this in a separate PamContext alongside the normal
# password transaction so either mechanism can complete independently.
auth required pam_fprintd.so
