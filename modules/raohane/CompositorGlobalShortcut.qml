import Quickshell.Hyprland

// Raohane is Hyprland-only. Keep the historical component name so native
// surfaces can preserve their existing shortcut declarations while avoiding
// the legacy compositor-selection wrapper from qs.services.
GlobalShortcut {
}
